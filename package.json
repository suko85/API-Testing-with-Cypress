{
  "name": "cypressapitesting-",
  "version": "1.0.0",
  "description": "API Testing with Cypress",
  "main": "index.js",
  "scripts": {
    "tests": "echo \"Error: no test specified\" && exit 1",
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build --prdo --base-href ./ && cp CNAME dist/CNAME",
    "test": "ng test",
    "lint": "ng lint --force",
    "e2e": "ng e2e",
    "cypress:run": "npx cypress run",
    "cypress:e2e": "start-test start http-get://localhost:4200 cypress run"
    
  },
  "keywords": [
    "QA",
    "Automation",
    "Cypress",
    "API",
    "Testing"
  ],
  "author": "suko85",
  "license": "ISC",
  "dependencies": {
    "cypress": "^7.5.0"
  }
}
